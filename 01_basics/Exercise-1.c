// Write a program that declares variables for a student’s age, and grade, assigns values to them, and prints them. 

#include <stdio.h>

int main(){
    int age = 20;
    char grade = 'A';

    printf("Age : %d\n", age);
    printf("Grade : %c", grade);

    return 0;
}